package org.valkyrienskies.tournament

import net.minecraft.resources.ResourceLocation

object TournamentLootTables {
    val BALLOON_POP = ResourceLocation(TournamentMod.MOD_ID, "special/balloon_pop")
}